{"id": "AUD-001", "severity": "HIGH", "status": "fixed", "component": "freeze", "title": "Freeze did not bind analytics, tiers, calendar, or data layer", "description": "research_freeze_v1 fingerprinted only strategy/portfolio/engine/ranking modules. metrics.py, validation.py (bootstrap/DSR/walk-forward), tiers.py, calendar.py, experiments.py, holdout.py, tax.py and the entire data layer (loader, importer, quality gate, security master, providers) could be edited after freezing without detection \u2014 every reported number, evidence tier, and gate decision was alterable post-freeze.", "reproduction": "audit probe 1: diff aitb.freeze._FROZEN_MODULES against behavior-defining modules", "expected": "any code that computes, tiers, gates or records a result is fingerprint-bound", "actual": "10 behavior-defining modules unbound", "impact": "convincing but unfrozen results possible; freeze guarantee vacuous for analytics", "affected_study_versions": ["v1"], "fix": "13 additional modules added to _FROZEN_MODULES; FREEZE_VERSION=2", "regression_test": "tests/test_audit_regressions.py::test_freeze_binds_analytics_and_data_layer", "freeze_bump_required": true, "invalidates_prior_results": "no real results existed under v1; synthetic demos regenerated"}
{"id": "AUD-002", "severity": "HIGH", "status": "fixed", "component": "freeze", "title": "Freeze verified only at experiment time, not at analysis/report time", "description": "run_robustness, run_capacity, run_company_analysis, make_report ran real mode without verify_freeze(). Rankings, robustness statistics, capacity estimates and the report itself could be produced by modified code after the experiments ran under the frozen code.", "reproduction": "audit probe 2: grep verify_freeze in real-mode scripts", "expected": "every real-mode entry point verifies the freeze", "actual": "5 of 6 entry points did not", "impact": "derived analytics and the decision surface were unfrozen", "affected_study_versions": ["v1"], "fix": "verify_freeze() added to all real-mode scripts", "regression_test": "tests/test_audit_regressions.py::test_all_real_mode_scripts_verify_freeze", "freeze_bump_required": true, "invalidates_prior_results": "none existed"}
{"id": "AUD-003", "severity": "HIGH", "status": "fixed", "component": "holdout", "title": "Holdout lock not tamper-evident: deleting the file restored clean status", "description": "holdout_lock.json was the only record; _load() returned a pristine state when the file was missing, so rm holdout_lock.json silently erased a compromised flag and the entire access log.", "reproduction": "audit probe 3: record two accesses (compromised=True), delete file, holdout_status shows compromised=False, 0 accesses", "expected": "append-only, tamper-evident; a compromised holdout stays compromised", "actual": "deletion fully reset the record", "impact": "holdout discipline unenforceable against a single file deletion", "affected_study_versions": ["v1"], "fix": "every freeze/access event is hash-chained and mirrored into the append-only experiment registry; holdout_status cross-checks lock vs registry and forces compromised on mismatch; events bind freeze hash + store fingerprint", "regression_test": "tests/test_audit_regressions.py::test_holdout_lock_deletion_is_detected, test_holdout_lock_rewrite_is_detected, test_holdout_events_bind_freeze_hash", "freeze_bump_required": true, "invalidates_prior_results": "none existed"}
{"id": "AUD-004", "severity": "HIGH", "status": "fixed", "component": "decision_brief", "title": "Missing capacity estimate treated as passing (fail-open)", "description": "The advancement check `(not len(crow)) or capacity >= 1e6` passed whenever a strategy had NO row in capacity.csv \u2014 an absent estimate counted as adequate capacity.", "reproduction": "audit probe 4: source inspection + logic table", "expected": "unknown capacity blocks advancement", "actual": "unknown capacity permitted advancement", "impact": "a strategy could advance to paper-trade without any capacity evidence", "affected_study_versions": ["v1"], "fix": "fail-closed: capacity must be present, finite and >= $1M; NaN/inf metric values also block via _finite()", "regression_test": "tests/test_audit_regressions.py::test_brief_fail_closed_source_contract", "freeze_bump_required": true, "invalidates_prior_results": "none existed"}
{"id": "AUD-005", "severity": "HIGH", "status": "fixed", "component": "decision_brief", "title": "Compromised holdout and failed/stale quality gate did not block advancement", "description": "The brief displayed a compromised-holdout banner but still computed survivors; it also never re-checked the gate status or whether the store fingerprint still matched the gate (results could be stale).", "reproduction": "audit probe 5: source inspection", "expected": "compromised holdout, non-passing gate, or fingerprint mismatch force DO NOTHING", "actual": "advancement logic ignored all three", "impact": "invalid evidence could produce a PAPER-TRADE recommendation", "affected_study_versions": ["v1"], "fix": "blocked_reason preconditions force DO NOTHING and empty the survivor list", "regression_test": "tests/test_audit_regressions.py::test_brief_fail_closed_source_contract", "freeze_bump_required": true, "invalidates_prior_results": "none existed"}
{"id": "AUD-006", "severity": "MEDIUM", "status": "fixed", "component": "metrics", "title": "CAGR annualized by row-count/252 instead of elapsed calendar time", "description": "cagr() used len(returns)/252 as 'years'. On the synthetic business-day calendar (~261 rows/yr) this overstated elapsed years and understated CAGR by ~35-40bp/yr (probe: true 10.00% reported as 9.63%). On the NYSE calendar (~250-253 sessions/yr) the error is small but nonzero.", "reproduction": "audit probe 6: 4-calendar-year b-day series with known 10% CAGR", "expected": "10.00%", "actual": "9.63%", "impact": "all CAGR/Calmar figures biased; cross-calendar comparisons inconsistent", "affected_study_versions": ["v1", "synthetic demos"], "fix": "elapsed (index[-1]-index[0]).days/365.25 when a DatetimeIndex is present", "regression_test": "tests/test_audit_regressions.py::test_cagr_uses_elapsed_calendar_time", "freeze_bump_required": true, "invalidates_prior_results": "synthetic demo artifacts regenerated; no real results existed"}
{"id": "AUD-007", "severity": "HIGH", "status": "fixed", "component": "lineage", "title": "Experiment records carried no data-store fingerprint", "description": "Real-mode experiment records contained only a universe hash \u2014 no binding to the validated store contents or governing freeze, so the lineage chain raw->checksum->import->store->run->record->report was broken at the store->record link, and a report could silently present results from a superseded store.", "reproduction": "audit probe 10: inspect record keys", "expected": "records bind store fingerprint + freeze hash; report invalidated on mismatch", "actual": "no such binding", "impact": "stale or substituted data undetectable downstream", "affected_study_versions": ["v1"], "fix": "records embed lineage {store_fingerprint, freeze_hash} in real mode; report gains a provenance section that flags fingerprint mismatches as STALE and lists artifact hashes", "regression_test": "tests/test_audit_regressions.py::test_real_records_carry_store_fingerprint", "freeze_bump_required": true, "invalidates_prior_results": "none existed"}
{"id": "AUD-008", "severity": "MEDIUM", "status": "fixed", "component": "statistics", "title": "Deflated-Sharpe trial battery excluded failed variants", "description": "run_robustness built trial_sharpes only from OK runs; errored variants (still trials) were dropped, understating N and making the DSR correction too lenient.", "reproduction": "audit probe 11: source inspection", "expected": "every attempted variant counts as a trial", "actual": "failed variants dropped", "impact": "DSR optimistic when failures occur", "affected_study_versions": ["v1"], "fix": "failed variants enter the battery as zero-Sharpe trials", "regression_test": "tests/test_audit_regressions.py::test_failed_variants_count_as_trials", "freeze_bump_required": true, "invalidates_prior_results": "none existed"}
{"id": "AUD-009", "severity": "MEDIUM", "status": "fixed", "component": "tiers", "title": "Tier assignment failed open: unknown/missing family promoted to Tier A", "description": "assign_tier fell through to Tier A for any record whose family was missing or unrecognized \u2014 omitted metadata produced the HIGHEST evidence tier.", "reproduction": "assign_tier({'family': '', 'status': 'ok'}, clean_gate) -> ('A', ...)", "expected": "missing tier metadata can never promote", "actual": "promoted to A", "impact": "misclassified evidence could reach the Tier A leaderboard and the brief's tier_A check", "affected_study_versions": ["v1"], "fix": "unknown family -> Tier B with explicit reason; missing status -> Tier C; dead 'or True' flag logic removed", "regression_test": "tests/test_audit_regressions.py::test_tier_metadata_omission_cannot_promote", "freeze_bump_required": true, "invalidates_prior_results": "none existed"}
{"id": "AUD-010", "severity": "MEDIUM", "status": "open_documented", "component": "ranking", "title": "Verdict layer alone admits ~3% of pure-noise strategies", "description": "30 random-return strategies against a noise benchmark: 1 achieved robust_candidate through the composite-score + relative-hurdle verdict (probe 7). The verdict layer is not the last line of defense \u2014 advancement additionally requires Tier A, bootstrap CI excluding zero, concentration and capacity checks \u2014 but the verdict label itself has a nonzero false-discovery rate.", "reproduction": "tests/test_audit_regressions.py::test_null_strategies_rarely_survive_verdict_layer (bounds it at <=2/30)", "expected": "near-zero false discovery at the final decision layer (achieved via stacked criteria)", "actual": "~3% at the verdict-label layer", "impact": "verdict labels in reports should be read as candidates, not conclusions", "affected_study_versions": ["v1", "v2"], "fix": "not fixed by design: tightening the frozen ranking criteria mid-audit would itself be a spec change; the layered advancement criteria are the control; documented in report", "regression_test": "tests/test_audit_regressions.py::test_null_strategies_rarely_survive_verdict_layer", "freeze_bump_required": false, "invalidates_prior_results": false}
{"id": "AUD-011", "severity": "MEDIUM", "status": "fixed", "component": "engine", "title": "No per-bar accounting invariant existed", "description": "The engine had endpoint tests but no continuous invariant tying equity changes to P&L + interest - costs each bar, so a future accounting regression could pass endpoint tests while leaking value mid-path.", "reproduction": "absence; hand-ledger probes 8a-c all reconciled exactly (no active defect found)", "expected": "audit mode asserts dEquity == overnight P&L + intraday P&L + interest - costs on every bar", "actual": "no such check", "impact": "latent-regression risk, not an active error (hand ledgers reconciled to the cent)", "affected_study_versions": ["v1"], "fix": "check_invariants flag / AITB_ENGINE_INVARIANTS=1 enables the per-bar invariant with 1e-6 relative tolerance", "regression_test": "tests/test_audit_regressions.py::test_engine_invariant_holds_on_stress_paths, test_engine_invariant_with_delisting", "freeze_bump_required": true, "invalidates_prior_results": false}
{"id": "AUD-012", "severity": "INFORMATIONAL", "status": "documented", "component": "engine", "title": "Dividends modeled as continuous reinvestment, not cash ledger entries", "description": "Ratio-adjusted (total-return-consistent) prices embed dividends in the price path; there is no discrete cash dividend credit. Internally consistent and benchmark-consistent, but it assumes frictionless same-security reinvestment and the tax overlay estimates dividend income from a configured yield rather than actual distributions.", "reproduction": "design reading + test_adjusted_vs_unadjusted_dividend_gap", "expected": "documented modeling assumption", "actual": "as documented", "impact": "small overstatement of compounding for dividend payers vs a cash-account investor; tax dividend estimate approximate", "affected_study_versions": ["all"], "fix": "documented in README and report methodology; discrete dividend ledger listed as future work", "regression_test": "existing dividend-gap test", "freeze_bump_required": false, "invalidates_prior_results": false}
{"id": "AUD-013", "severity": "INFORMATIONAL", "status": "documented", "component": "freeze", "title": "Freeze does not bind dependency versions or in-process monkey-patching", "description": "pip freeze is recorded in the run fingerprint but not enforced; a different pandas/numpy could alter numerics. In-process monkey-patching after verification is unpreventable in Python. Both are recorded (fingerprint) rather than enforced.", "reproduction": "design reading", "expected": "recorded and disclosed", "actual": "recorded in run_fingerprint.json", "impact": "reproducibility depends on honoring the recorded environment", "affected_study_versions": ["all"], "fix": "documented; strict pin-file enforcement listed as future work", "regression_test": "n/a", "freeze_bump_required": false, "invalidates_prior_results": false}
{"id": "AUD-014", "severity": "LOW", "status": "fixed", "component": "tiers", "title": "Dead logic in gate_flags (macro_revised '... or True')", "description": "The macro_revised flag was computed then overridden by 'or True' \u2014 harmless (FRED is always revised) but misleading.", "reproduction": "source read", "expected": "clear constant", "actual": "dead expression", "impact": "maintainability", "affected_study_versions": ["v1"], "fix": "replaced with explicit constant True + comment", "regression_test": "covered by tier tests", "freeze_bump_required": false, "invalidates_prior_results": false}
{"id": "AUD-015", "severity": "INFORMATIONAL", "status": "documented", "component": "ranking", "title": "Composite score includes holdout Sharpe by design", "description": "The ranking's 2x holdout-Sharpe term means the final ordering uses holdout results. This is the sanctioned single final evaluation (spec frozen before any real holdout view, one logged access), not selection leakage \u2014 but it must never be described as out-of-sample model selection, and re-running ranking repeatedly against holdout data would erode its validity (now mitigated by the tamper-evident access log and the compromised flag blocking advancement).", "reproduction": "ranking.py score_record", "expected": "explicit, labeled, single-access final evaluation", "actual": "as designed; reports label holdout columns", "impact": "interpretation discipline required", "affected_study_versions": ["all"], "fix": "documented in audit report and methodology", "regression_test": "holdout access controls", "freeze_bump_required": false, "invalidates_prior_results": false}
{"id": "AUD-016", "severity": "HIGH", "status": "fixed", "component": "experiments", "title": "Experiment ID omitted the universe, so an expanded roster reused old results", "description": "experiment_id() hashed (spec, provider, data_mode, scenario, data_span) but NOT the investable universe. run_experiment skips any ID already in the registry and reloads its stored equity curve, so re-running an unchanged strategy against an EXPANDED universe returned the previous universe's results while recording them as current. The defect could not fire inside a single freeze, because the freeze holds the universe fixed \u2014 it fires precisely at a freeze boundary that widens the roster, which is exactly when old and new numbers are most likely to be compared.", "reproduction": "expand configs/universe.yaml; re-run scripts/run_experiments.py; every pre-existing variant is skipped as cached despite a different investable set", "expected": "a run against a different universe is a different experiment with a different ID", "actual": "identical ID; cached curve from the narrower universe reused and presented as the new result", "impact": "silently stale results at every universe expansion; cross-universe contamination of every downstream ranking, scorecard and report", "affected_study_versions": ["v1", "v2"], "fix": "experiment_id() now binds universe_hash (tickers plus listing windows) and freeze_version; records carry universe_hash and universe_size; ranking.current_cohort() and platform.catalog.current_registry() restrict every leaderboard, scorecard and comparison to one universe cohort; superseded cohorts are retained and labelled, never deleted", "regression_test": "tests/test_audit_regressions.py::test_experiment_id_binds_universe", "freeze_bump_required": true, "invalidates_prior_results": "no real results existed under v1 or v2; the synthetic v2 cohort is retained in the registry and excluded from all comparisons by universe hash"}
{"id": "AUD-017", "severity": "HIGH", "status": "fixed", "component": "experiments", "title": "Append-only registry has no uniqueness guard on experiment IDs", "description": "ExperimentRegistry.append() writes unconditionally. Two runner processes started against the same registry both pass the existing_ids() check (read once at entry) and then both append records for the same IDs. Observed during the freeze-v3 run when a background launch was mistakenly issued twice: 718 records were written where 359 were expected, every ID duplicated, and concurrent parquet writes to the same curve path are not atomic either. Values were identical because the pipeline is deterministic, so no number was wrong \u2014 but headline counts double and curve files are potentially torn. RECURRED on the first real-data study (2026-08-05): three overlapping runs \u2014 the run_first_real_study.sh invocation plus two manual pipeline chains \u2014 wrote 1,644 records for 646 experiments, a 2.5x inflation of every count. All duplicates agreed exactly on every metric, so no reported number was wrong, but the concurrent store access also tripped the gate's fingerprint check and blocked make_report and run_company_analysis.", "reproduction": "start scripts/run_experiments.py twice concurrently against one registry root; inspect duplicate IDs in results/<mode>/experiments.jsonl", "expected": "a second append of an existing ID is refused, or the runner refuses to start while another holds the registry", "actual": "both processes append; duplicates are only removed at display time", "impact": "inflated run counts and torn curve files; no incorrect metric, because runs are deterministic", "affected_study_versions": ["v1", "v2", "v3"], "fix": "scripts/run_experiments.py takes an O_EXCL lock on the registry root (.runner.lock) and exits with an explanatory message if another runner holds it. Deliberately NOT fixed inside ExperimentRegistry.append(): experiments.py is bound by the research freeze, and changing it would supersede the freeze the first real study ran under, forcing a full re-run to fix a bookkeeping defect. The lock removes the only way the condition arises. The real registry was de-duplicated (first write wins; all copies byte-equal on metrics) and tests/test_audit_regressions.py::test_registry_has_no_duplicate_ids covers both registries.", "regression_test": "tests/test_audit_regressions.py::test_registry_has_no_duplicate_ids", "freeze_bump_required": true, "invalidates_prior_results": "no \u2014 duplicates were exact copies, so every metric, ranking and verdict is unchanged; only record COUNTS were inflated, and the registry has been de-duplicated in place"}
{"id": "AUD-018", "severity": "HIGH", "status": "fixed", "component": "strategies", "title": "Strategy family labelled by source file, not by construction", "description": "Five long-only screens (GrossProfitability, ResearchIntensity, AccrualQuality, PostEarningsDrift, DispersionTimedSelection) were assigned family='longshort' because they were authored in longshort.py. `family` is part of strategy.spec() and therefore of the experiment ID; it also groups the deflated-Sharpe trial batteries in run_robustness.py and drives the correlation-by-family chart on the site. The mislabelling made the site state that hedged strategies show a median correlation of 0.83 to the index \u2014 the exact opposite of the finding \u2014 because five long-only members dragged the family median up. The genuinely hedged strategies measure about zero (market-neutral book: -0.005 monthly correlation to QQQ across all cost scenarios).", "reproduction": "compare each class's `family` attribute against whether its build() can emit a negative weight; render _independence_section and read the family median", "expected": "family describes the construction; only strategies able to hold a negative position are 'longshort'", "actual": "family described the source file; the site's central conclusion was inverted", "impact": "site published a false headline conclusion; deflated-Sharpe trial batteries grouped unrelated strategies together, and after the correction the same strategies briefly appeared under two family labels, splitting their batteries and overstating significance on both sides", "affected_study_versions": ["v4 (pre-correction, synthetic only)"], "fix": "families reassigned to longshort / quality / factor; experiments.yaml split into longshort, quality and meta_timing groups; freeze v4 reissued (hash 384dc48714fc530de3f3a4a9168df12f) before any real-data run; 36 superseded synthetic records whose recorded family contradicted the class definition were removed from the registry.", "regression_test": "tests/test_audit_regressions.py::test_strategy_family_describes_what_it_is, ::test_market_neutral_family_really_is_uncorrelated, ::test_registry_families_match_the_current_classes", "freeze_bump_required": true, "invalidates_prior_results": "no real results existed under v4; the affected synthetic records were removed and the grid re-run. The v3 real study is unaffected \u2014 the longshort family did not exist under v3."}
{"id": "AUD-019", "severity": "HIGH", "status": "fixed", "component": "site", "title": "A synthetic site rebuild silently replaced the published real-data site", "description": "build_site() renders both data modes into the same directory (site/). A routine synthetic rebuild during freeze-v4 development overwrote the published real-data site, so a page that had been showing 624 real experiments reverted to 'Simulated data \u2014 real study not run'. Nothing errored. The only symptom was a banner nobody had reason to re-read, on a site that had been correct the day before. A related failure was found while fixing it: building in REAL mode on a machine without the price store renders confidently and wrongly \u2014 '0 companies in the test', empty correlation analysis \u2014 under a banner reading 'Real market data', which is worse than not building at all.", "reproduction": "build_site('real'); build_site('synthetic'); read the status pill. Or build_site('real') on a fresh clone, where prices and curves are gitignored.", "expected": "a lower-fidelity build cannot replace a higher-fidelity one without explicit intent; a real build without real data refuses rather than renders", "actual": "silent overwrite in one direction, confident wrong numbers in the other", "impact": "published site misrepresented the state of the research in both directions \u2014 claiming no real study had run when one had, and claiming real data when none was present", "affected_study_versions": ["v3", "v4"], "fix": "site/ carries a .site_mode stamp. A synthetic build over a real site raises unless allow_downgrade=True. A real build refuses when the price store or the equity curves are absent, with the command to run it where the data lives. Site restored from the last real build.", "regression_test": "tests/test_real_mode.py::test_synthetic_build_cannot_clobber_a_real_site", "freeze_bump_required": false, "invalidates_prior_results": "no \u2014 presentation only; no recorded result was altered"}
{"id": "AUD-020", "severity": "HIGH", "status": "fixed", "component": "validation", "title": "AUD-008's failed-trial battery fix was silently inert", "description": "AUD-008 established that failed variants must enter the deflated-Sharpe trial battery as zero-Sharpe trials, so the multiple-testing correction is not understated by dropping them. The implementation matched failures on a top-level `family` column \u2014 but ExperimentRegistry failure records carry only `spec`, with no family, universe_hash or freeze_version. n_failed was therefore always 0, and the AUD-016 cohort filter (added later) dropped failure rows entirely before that code was even reached. Every family's deflated Sharpe has been overstated since AUD-008 was declared fixed. Observed live on the first freeze-v4 real study: 48 failed runs, 33 in the current cohort, every one invisible to the correction. Found while investigating why two v4 quality strategies produced no records \u2014 they had correctly refused to run against a pre-v4 fundamentals store, and their refusals were nowhere in the family counts.", "reproduction": "run a study where some variants raise (e.g. strategies requiring fundamental fields the store lacks); inspect n_failed in run_robustness for their family \u2014 always 0", "expected": "every attempted variant counts as a trial, whether it succeeded or not", "actual": "failures were dropped twice: by the cohort filter, then by a match on a column that failure records never have", "impact": "deflated Sharpe overstated for every family in every study; strategies looked more statistically significant than the evidence supports", "affected_study_versions": ["v2", "v3", "v4"], "fix": "scripts/run_robustness.py::recover_failed_trials derives family from spec and selects failures inside the current cohort's execution window (exact, because a study runs as one batch). Implemented in the script rather than experiments.py so it needs no freeze bump and does not supersede the freeze the real study ran under. A second guard was added alongside: run_robustness now refuses to run when no equity curves are present, rather than writing an empty summary over a good one \u2014 which is how a real robustness summary was destroyed while diagnosing this.", "regression_test": "tests/test_audit_regressions.py::test_failed_trials_are_recovered_for_the_multiple_testing_correction", "freeze_bump_required": false, "invalidates_prior_results": "no result is wrong, but every published deflated-Sharpe probability predates the fix and is optimistic. Re-run scripts/run_robustness.py to regenerate them; no experiments need re-running."}
